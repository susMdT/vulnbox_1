version: '3.1'

networks:
  static-network:
    ipam:
      config:
        - subnet: 10.10.10.0/16
services:

  db:
    image: prod
    ports:
      - "3306:3306"
    networks:
      static-network:
        ipv4_address: 10.10.10.10

    restart: always
